# Legacy stub: primary RTD config lives in PteroSimScripting/.readthedocs.yaml
# (Option A — docs build from the scripting repo with this tree as a submodule).
#
# Building this repository alone will fail: conf.py expects
# ../SDK/python (PteroSimScripting layout).
version: "2"

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.10"

python:
  install:
    - requirements: docs/requirements.txt

sphinx:
  configuration: docs/source/conf.py
